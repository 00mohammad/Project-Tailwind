{
  "name": "project-tailwind",
  "version": "1.0.0",
  "scripts": {
    "build": "npx @tailwindcss/cli -i ./src/input.css -o ./public/styles/app.css --minify",
    "watch": "npx @tailwindcss/cli -i ./src/input.css -o ./public/styles/app.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.1"
  }
}